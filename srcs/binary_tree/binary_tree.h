#ifndef BINARY_TREE_H
# define BINARY_TREE_H

/* where the binary tree structure of the command line arguments string
 * execution order will be implemented 
 * 
 * currently the idea is to create a strucure so it represents properly the
 * order in which the commands will be eecuted and how, if they are gonna
 * execute alone, if they are gonna execute and pipe to the next cmmd etc..*/

#endif
