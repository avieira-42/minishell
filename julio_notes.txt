env command

if env with no args:
	print all current environment variable
else:
	if the variable exists:
		if new value is larger than old value:
			allocate more space
		set the value and nul terminate
	if the variable does not exists:
		allocate more space for the envp
		allocate new space inside envp and at the bottom
		set the value and nul terminate
